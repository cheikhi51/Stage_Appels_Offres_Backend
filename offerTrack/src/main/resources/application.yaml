spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AppelsOffres
    username: postgres
    password: hamadach2003
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
